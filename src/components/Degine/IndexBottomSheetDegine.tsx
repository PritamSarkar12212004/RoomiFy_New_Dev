import { View, Text, FlatList, TouchableOpacity } from "react-native";
import React, { useState } from "react";
import BottomSheetSearch from "../button/BottomSheetSearch";

const IndexBottomSheetDegine = () => {
  return (
    <View className="w-full pt-5">
      <BottomSheetSearch />
    </View>
  );
};

export default IndexBottomSheetDegine;
